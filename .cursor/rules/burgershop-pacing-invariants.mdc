---
description: 节奏内核不变量——改动成本/产能/进度/解锁/变现时必须满足的下限
alwaysApply: false
---

# 节奏内核不变量（约束，**不授权新功能**）

> **本文件不授权任何新功能。** 玩家可见行为仍必须有 `docs/specs/` 里的 spec（见 `burgershop-core.mdc`），本文件不构成 spec。
> 本文件只规定：**当你按 spec 改动经济、进度、解锁或变现时，必须同时满足的下限条件。**
> 与 `AGENTS.md`、`burgershop-core.mdc`、`burgershop-editmode-gate.mdc` 并用，不替代它们。
>
> **关于事实部分**：下方「已知差距」是 **2026-09-13 对源码的快照**，由外部审查得出、经核对修正。
> **若你发现某条与代码不符，以代码为准**，并在回复中指出——不要为了让本文档成立而迁就它。
> 事实会随开发漂移，不变量不会。
>
> **完整推理、四台时钟、七层反馈栈与核实基线见 `docs/GAME_CORE.md`。** 本文件是它的可执行压缩版；
> 需要理解「为什么」时读那一份，需要判断「能不能这么做」时用本文件。

## 一句话内核

**让玩家在任意一秒都处于「差一点就能拿到下一个东西」的状态，并且从不给他一个「这里可以停」的理由。**

## 当前项目的已知差距（不要假装不存在，也不要无 spec 擅自修）

| 差距 | 证据 |
| --- | --- |
| **阶梯会终止**：最长 5 级（玩家与员工的速度/搬运共 4 条），多数设施 2–3 级，餐桌是**一次性三选一**（80 金币）。全部金币升级共 **44 次、9,410 金币**；含建造与雇满共 **12,610 金币** | `PlayerBoost.cs:7,12`、`StaffBoost.cs:8-11`、`ExpandableGrill.cs:91`、`GrowthUpgrades.cs:66,71`、`BagLine.cs:114`、`TableSetCatalog.cs:42`、`ShopExpansion.cs:12-21,143-261` |
| **终点态是默认终局**：最高级只有 5 级，玩家在几十单内就会见到 MAX | `BoostUpgradeZone.cs:109`、`WorkerHiringZone.cs:400`、`SessionGoalTracker.cs:47`。（注意：多数世界文字的 MAX 卡片由 `WorldLabelHud` 接管，别只凭字符串判断玩家可见性 —— `WorldLabelHud.cs:41`） |
| **无跨会话时间机制**：存档无时间戳，无离线收益。会话内的生产间隔（如 3/2/1.5 秒）是另一回事，不算此条 | `RestaurantSaveData.cs:11-56` |
| **Rank 6 后目标系统不再组织长期追求**：剩余建造做完后回落为杂务提示。不是"立即只剩杂务" | `ShopRanks.cs:33`、`SessionGoalTracker.cs:235-309` |
| **零件只有来源、没有消费口**：骑手订单产出零件并可拾取入账，但 `PartsWallet` 无 `TrySpend`。**这是 BS-SPEC-036 的用户决定："先只收集、计数、存档，不消费或兑换"** —— 不是遗漏，实现方不得擅自推翻 | `CourierLine.cs:16,207-217`、`PartsWallet.cs:6`、`docs/specs/BS-SPEC-036.md:3` |

**两条容易被误用的口径：**
- 单价并非统一 10 金币：堂食 10、汽车每件 15、纸袋每件 20、骑手每包 20。用"单数"比较时只是统一口径，不是实际订单数。
- 旧餐桌 `150/300/600` 的定义仍留在 `GrowthUpgrades.cs:56`，但存在 `TableUpgradeBoard` 时该入口被过滤（`GrowthUpgrades.cs:29`）——**是死代码，不要按它计算**。

**这些差距只能由用户决定的 spec 来修，不能由实现方自行"顺手优化"。**

## 八条不变量

- **I1 目标常在**：任意时刻玩家都有一个「可见 + 当前可推进」的目标。
- **I2 没有死路**：不存在「必须干等且无事可做」的状态；超过 90 秒的等待要有可选加速出口。
- **I3 永远不满**：长期进度**不显示 100%**，但**必须显示还差多少**。新增梯子时默认预留 15 级以上的空间（具体值由 spec 决定），不要用 MAX 收尾。
- **I4 变化可感知**：每次升级/解锁至少产生一处可见变化（外观优先于数字）。本项现有实现良好，保持。
- **I5 改写而非并列**：新机制必须改写已有循环的参数，不得作为孤立平行系统；**新增货币前必须先回答"它的来源与消耗口分别是什么"**。
- **I6 离线与奖励按比例**：一切"授予进度"的东西（未来若有离线收益/广告奖励/道具/礼包）必须按当前 T(n) 计价并封顶，**禁止固定数值**。当前无离线收益，是否引入由用户 spec 决定 —— 本条不要求新增离线系统。
- **I7 广告/内购是加速器**：若引入，广告时间 ≤ 会话 15%、首曝不早于 3 分钟，核心进度不得锁在广告或付费后。
- **I8 停止由玩家决定**：靠「还有一步」实现，不靠「卡住」实现。

## 涉及节奏改动时的设计检查

1. 本次改动服务哪台时钟？（比率 / 间隔 / 开放回路 / 变动比率；不涉及则注明）
2. 给的是「探索」（新动词/新决策）还是「规模」（更大数字/更好看外观）？
3. 会改变 T(n) 吗？改动前后的 T(n) 是多少？（T(n) = 攒够下一级所需时间）
4. 是否制造了「无出口的等待」或「可停下的时刻」？
5. 是否引入孤立平行系统？（是 → 重做）
6. 是否触碰 I3/I7 红线？

已有 spec 或源码能回答的直接核对；不涉及的注明不适用。可通过局部测量确定的先测量，仅对无法推断且会改变产品取舍的决定询问用户，同时推进独立工作。PR / 提交信息只记录实际相关的结论。

## 本项目的硬禁令

- 没有 spec 时**不得**增删任何成本数组、等级上限、员工数上限
- **不得在其它脚本里新增硬编码数值**（新数值一律集中到一处配置，不要继续散落在内联数组里）
- 不得为了让功能"更完整"而把某个梯子标成 MAX 作为默认终态
- 不得保留无来源或无消耗口的货币（**已有用户决定的除外**，例如零件当前只收集不消费）
- 不得用固定值实现任何进度类奖励
- 不得为了让改动通过而修改既有测试的断言 —— 先核对失败原因和规格；若已批准的规格改变了旧预期，可更新对应断言并说明依据，禁止仅为变绿删除或放宽检查

## 完成定义

- [ ] 相关设计问题已有证据或规格依据；无关项不强制展开
- [ ] 按 AGENTS.md 完成本次相关 EditMode / 本地验证；确有跨模块影响时扩大回归
- [ ] 若动了任何数值：附改动前后 T(n) 对照，并说明由哪份 spec 授权
- [ ] 新机制改写的是哪个已有参数（不是新增平行系统）
- [ ] 未触碰 I3/I7 红线
