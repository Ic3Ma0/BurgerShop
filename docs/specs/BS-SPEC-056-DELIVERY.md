# BS-SPEC-056 轻度对齐吸附交付

- 新增 PlacementAlignment：基于设施本地占地框与实际朝向计算近邻的平行、中心线、前后边线对齐；兼容不同尺寸、偏心模型和180度反向朝向，保持面向，不强制掉头。
- 参数集中：位置误差0.22、角差6度、邻边距离1.4、停稳0.25秒（累计移动不超过0.06）。相邻边贴近时留0.12间隙，拖动原始点超过0.5立即解除。
- FacilityShopHud 的新购/移动共用预览接入；原始拖拽姿势与吸附姿势分离。只有停稳触发时搜索近邻并尝试一次完整 CanPlace，不每帧进行吸附寻路。非法姿势保留原自由位置。
- 青色对齐线+Aligned提示；手动旋转解除吸附，UI悬停/触摸不再新增吸附，双指移镜头暂停吸附。Done保持提交已显示的姿势。桌面拖开与点击同帧也先解除旧吸附，避免提交上一位置。
- 未改购买价格、商城目录、边界/通路校验、保存格式；运行中角色/暂停行为沿用现有逻辑。

验证：Unity6000.3.23f1、隔离副本/tmp/bs041-baseline、隔离存档，真实玩家存档未修改。

**23/23通过**：PlacementAlignmentTests + Spec043ShopTests + Spec043PlacementTests，证据 Logs/spec056/editmode-results.xml。

覆盖旋转/偏心/不同尺寸的精确边线；反向平行不掉头；远处、错开、大角度不吸；60帧快速移动不锁定；停稳与脱开滞回；真实SampleScene中非法边界吸附拒绝、合法对齐、同帧拖开、重新吸附、UI经过冻结、Done精确提交并保存。

首次布局测试选用的桌子侧面间隙会挡住座椅通路，被原有CanPlace正确拒绝；未放宽校验，改用座位可达的合法前后排列验证吸附成功。对应边界拒绝仍有独立断言。

本地渲染已检查：Logs/spec056/alignment.png（绿色预览、青色线）。参数属于首版保守手感；Android触屏真机体验未在本轮实测。git diff --check通过。未提交GitHub。
