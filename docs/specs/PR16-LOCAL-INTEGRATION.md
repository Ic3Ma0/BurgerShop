# PR16 餐桌功能本地整合

日期：2026-09-13

## 范围与状态

按用户确认“整合进当前最新版本，保留已有功能”，将 `origin/feat/shop-rank`（a033aeb）整合进 `feat/growth-gameplay`（整合前 13a6c05）。保留可乐、骑手、独立零件货币、自动传送带和蓝色汽车柜台。仅本地合并；没有推送、合并 GitHub main 或发布 APK。

新增餐桌套装选择与分次投入、四人桌和方桌购买，以及桌椅、柜台等实体碰撞。原有 specs 030–032 与远端重复编号，保留本地版本，远端文本另存为 PR16-BS-SPEC-030/031/032.md。

## 整合处理

- 远端餐桌占用 v8/v9，与本地等级/成长存档冲突；餐桌新字段改为 v12，继续按原版本计算 v1–v11 校验，保留可乐及零件余额等现有字段。
- 旧餐桌升级收益取既有收益与新套装收益的较大值；旧升级等级继续恢复。挂接新餐桌面板后隐藏旧餐桌升级入口，避免出现两套扣款入口。
- 保留顾客入座时锁定餐费，换装不改变已经入座的订单收益。
- 保留已有蓝色汽车柜台布局；新增碰撞应用在当前柜台上，不恢复远端旧窗口模型。
- 餐桌采用远端参数：换装 80，四人桌 200，方桌 150；基础用餐 3 秒，Bistro 12/3 秒，Diner 12/2.4 秒，Patio 15/2.7 秒。未扩展新的玩法。
- 修正远端 PlayerClippingTests 缺少 Customer 命名空间导致的编译错误。

## 本地验证

Unity 6000.3.23f1 EditMode 首轮 333 项，324 通过、9 失败。处理后原失败项全部定向复验通过，最终无已知失败项；未声称修复后重新全量运行。

- 首轮结果：`Logs/pr16/results.xml`。
- 13 项场景/布局复验：`Logs/pr16/retest.xml`，11 通过；剩余员工两项后续通过。
- 69 项餐桌/存档/员工复验：`Logs/pr16/final.xml`，67 通过；员工两项见下。
- 最后员工定向复验：`Logs/pr16/worker-final.xml`，2/2 通过。
- 旧厨房场景测试暂停独立骑手经济，员工单线测试关闭可乐顾客区域，避免独立回路影响精确计数；正式游戏没有暂停这些功能。新增实体碰撞后旧键盘路线改为绕开烤台，继续断言实际到达位置。员工完成原定单量还需清桌和倒垃圾，等待上限分别调到 240/150 模拟秒，成交断言保留。
- 覆盖桌椅碰撞体与胶囊投射、投币区域可通行、购买及换装、部分投入恢复、存档校验，以及 SampleScene 键盘营业/升级/重启回归。测试使用隔离存档。

GUI 验收未完成：Unity 已启动，但 Mac 锁屏，界面工具无法操作。尚未手动在 Game 视图绕桌椅走动或目视验收换装，不把自动化物理断言作为视觉验收。解锁后可用 BurgerShop → Growth Preview (isolated save) 打开有测试资金的 SampleScene；正常 Play 使用真实存档。

按用户要求未运行 Android 验收。未改真实存档，保留 _Recovery 及此前无关工作区修改。
