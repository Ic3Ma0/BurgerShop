# BS-SPEC-090：人物避让实体设施

- 状态：已实现，定向自动验证通过，人工手感待复验；用户已确认所有人物不可穿过实体家具，摆放后立即绕行。
- 范围：玩家碰撞、顾客与员工导航、移动/新购设施提交后的障碍更新。保留现有经济、存档和工作分工。

## 行为与验收

1. 玩家移动被机器、桌椅等实体挡住，仍可沿边缘走开。
2. 员工与顾客绕过实体到达可用交互点；路径不通时等待，不能退化为穿过家具的直线。
3. 新购、移动、旋转设施提交后重新规划；不瞬移人物，不允许设施放在人身上。
4. 清桌、丢垃圾、取货和交付保持可达；保留库存与任务状态。
5. 验证旋转家具、圆柱障碍、动态布局变更、长帧及无路情况。场景人工试玩与自动测试分开记录。

## 技术边界与方案审查

- 归属与复用：复用 FacilityLayout 的布局版本和 LayoutNavigation；共享实体检测负责移动安全，角色原模块保留任务与队列。
- 状态归属：布局独占障碍缓存，角色独占当前路线进度；不新增持久化字段。
- 依赖方向：角色读取布局与物理实体；导航不修改订单、钱包和存档。
- 兼容与失败：旧档照常加载；失败保留人物位置并等待重试，不能用直线穿越作为兜底。通用导航必须精确到达目标；实体内部的目标判不可达，工作模块负责提供明确外侧交互点。
- 自动防线：定向 Unity 测试验证实体检测、路线分段、动态刷新和角色移动；执行 scripts/check-boundaries.sh。本轮不进行 Android 打包。

## 技术执行与交接

- 090 初版随 `7b4d8fd` 进入分支；本轮完成柜台移动保留顾客世界位置、用餐中禁止搬桌、机器升级刷新障碍，并补足证据。
- 定向验收 25 项最终通过；员工/商城扩展回归仍有已复现的既有失败，未标记全量通过。
- 证据与手工复验步骤：[090 交付记录](verification/090-obstacles/DELIVERY.md)。
- 分支 `fix/facility-obstacle-routing`，PR #20；未合并、未进行 Android 验收。

## PR #20 审查修订（P1 / P2）

- P1：CanReach 与 Route 共用严格的起终点连接检查；最后一段不可走时返回不可达/null，不把邻近网格当成交互点。垃圾桶等实体中心改用明确的外侧工作点。
- P2：入座与离座各有连续移动阶段，就餐计时在入座完成后开始，离座完成后释放座位。仅在使用本人椅子的短距离动作中允许进入该椅子，仍检查其他实体。
- 技术边界沿用上节：导航独占路径有效性；CustomerAgent 独占就餐阶段与移动进度；DiningTable 提供椅侧点及椅子身份，不修改顾客状态。
- 自动防线补充：最后一段阻塞/清晰终点对照断言，真实桌椅逐帧入座、吃饭、离座的位移上限及最终离店测试；复验清桌与员工丢垃圾。

- P1/P2 已修复并完成定向验证：68 项中 67 通过，1 项既有员工读档失败；[本轮结果、轨迹与场景画面](verification/090-obstacles/pr20-review/DELIVERY.md)。
