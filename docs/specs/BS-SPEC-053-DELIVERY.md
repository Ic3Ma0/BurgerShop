# BS-SPEC-053 卫生间扩建交付

- 大厅南侧、大门与汽车区之间新增独立卫生间。入口已修订为右上角超市首行 Restroom expansion，Lv4 起点击花300金币扩建（含2个隔间和洗手台），重复不会扣费。已有物品价格不变。
- 偶数编号堂食顾客餐后使用，两个隔间独占，最多2人在等候；队列已满则直接离店，等待超过20秒也离店。使用4秒后变脏，洗手1秒后从大门离开，不收额外费用。
- 用户已选择清洁机制：脏隔间停用并显示 CLEAN；玩家靠近累计3秒清洁，已有具备清洁能力且未携带物品的员工会预约、寻路并清洁，再回原经营流程。预约避免重复派工；脏污独立记录。暂停建造时计时和移动暂停。
- 存档v17保存扩建、投资、两隔间脏污位；兼容原v1..16校验和。不存临时客流、队列和清洁进度。051测试的迁移目标版本断言改为 CurrentVersion，收益公式/断言不变。
- 门与新增地面接入寻路、墙体碰撞；卫生间只属于人物活动地面，不开放放置营业设施。既有摆放校验增加卫生间门前保留区域与入口可达检查。隔间门在顾客到位后关闭，空闲时打开。

验证：Unity 6000.3.23f1，隔离副本 `/tmp/bs041-baseline` 与临时存档；真实玩家存档未动。

`Logs/spec053/editmode-results.xml`：**117/117通过**（卫生间3项、顾客队列、堂食/垃圾、员工、存档、摆放和离线收益）。

卫生间专项覆盖：
1. 扩建前未建造；投资一次扣300，再次不扣。
2. 大厅到入口、两个隔间、洗手台路径均存在。
3. 两隔间独占、第三客等候；使用后变脏，清洁完成前不可再分配。
4. 玩家距离检测与3秒累计清洁。
5. 顾客通过既有堂食流程进入卫生间、洗手并离店，结束后无残留预约。
6. 员工实际寻路到隔间清洁，并返回大厅。
7. 扩建/脏污落盘，重新加载SampleScene仍保留。
8. v16校验和不受新字段影响，v17非法脏污值拒绝。

本地渲染已查看：`Logs/spec053/restroom.png`。`git diff --check`通过。未运行Android或真机，未提交GitHub。

节奏：本刀给堂食循环增加餐后服务与清洁工作；不引入货币/额外收入，不改现有升级曲线。新扩建300的攒钱时间为300/实际净金币速率，速率依玩家经营而异，本轮未编造实测分钟数。忙碌或脏污不会无限扣住顾客；清洁可由玩家主动完成，后续由员工承担。

购买入口修复：撤掉不可发现的地面购买圈，卫生间固定显示在超市第一行。低于Lv4明确显示解锁等级，余额不足禁用，已建成显示Built；点击直接建成并关闭商城恢复时间。原投资字段继续保留与抵扣。卫生间/原商城12项通过，锁定状态、余额不足、旧投入100只收剩余200的专项另1项通过；证据 shop-purchase.xml、investment-credit.xml。未改300总价或等级条件。

新版卡片商城兼容修复：054卡片改版遗漏了卫生间入口的创建/刷新，只保留字段。现将BuyRestroom置于标签/提示下方、商品Viewport上方的固定条目，并调整Viewport及响应式宽度；未将固定扩建混入可拖拽设施目录。12级、旧投入100的场景验证真实EventSystem射线最前命中该按钮，点击扣剩余200、建成、关闭商城。等待Canvas首帧布局后专项通过；其他12项回归通过。渲染已核对 `Logs/spec053/card-shop-restroom.png`，射线/扣款结果 `card-shop-pointer.xml`。早先回归有1项因未等待首帧Canvas布局射线无结果，已修正测试时序并通过复验。
