# PR #17 可乐回路：本地集成记录

日期：2026-09-12。远端来源 `cursor/cola-product-2c90` / `3079b8f`。

已合入本地 `feat/growth-gameplay`，保留 030–035 和移除任务/携带面板的改动。未推送、未合入 GitHub main，未执行 Android 构建或验收。

## 修复与兼容

- 原分支基于 0.2.0；合并保留当前经营节奏、特殊顾客、临时容量倍率、袋装生产和手动星级系统。
- 可乐成为独立携带类型，不进入汉堡装盒/装袋配方；员工供货在堂食汉堡、盒装、袋装、可乐四线轮转。可乐机升级纳入独立星数记录。
- 存档使用 v10，追加可乐等级，保留已有 v1–v9 校验和迁移；没有采用云端与现有等级存档冲突的 v8 格式。
- 原可乐柜台 (3, 0, 4) 阻挡既有烤炉→汉堡柜台通路，调整为 (3, 0, -3)，白圈 (6.6, 0.02, -3)，队列向南排列；旧键盘行走回归已通过。
- 统一 HUD 增加可乐杯图标，保留用户要求删除的任务/携带面板状态。
- 云端重复使用 BS-SPEC-026 编号，可乐原规格保存在 `COLA_PR17_SPEC.md`，原 UI 026 文档和用户未提交修改保留。

## 本地验证

Unity 6000.3.23f1 编译通过。全套 292 项 EditMode（包含 EnterPlayMode 场景测试）初跑 278 通过，修复后全量复跑 283 通过；针对剩余问题复测 31 项通过 27 项，最终 4 项复测全通过。汇总每项最新结果：292/292 通过，并非声称最后重新全跑了一次。

覆盖可乐生产、混合携带/容量、对应柜台、逐件交付/结算、独立出餐节流、升级、员工供货和存档；已有汉堡/盒装/袋装测试也完成回归。旧单线场景测试改为明确选择汉堡设施，员工总成交断言计入新增可乐线；删除的 HUD 不再作为必需对象。

日志与 XML：`Logs/cola-integration/`。实际 GUI 打开 SampleScene 并进入 Play，看到可乐堆叠生产、可乐顾客与杯子订单气泡；检查 `play.log` 未发现运行异常。Unity 留在 `BurgerShop → Growth Preview (isolated save)` 创建的 5,000 金币 Lv6 体验存档，停止 Play 后恢复真实存档路径。

目前可以本机体验。视觉模型沿用该 PR 的简易设备造型，完整主观手感/外观由用户体验反馈；本次不声称 Android 已通过。
